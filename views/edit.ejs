<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit a Book</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>
<body>
    <form action="/" method="GET" class="m-3">
        <button type="submit" class="btn btn-outline-danger">< Back</button>
    </form>
    <div class="container m-4">
        <h2 class="mb-4">Add a New Book</h2>
        <form action="/edit/<%= book.id %>" method="POST">
            <div class="mb-1">
                <label for="titleInput" class="form-label">Title</label>
                <input type="text" class="form-control" id="titleInput" name="title" 
                value = "<%=book.title%>" required>
            </div>
            <div class="mb-1">
                <label for="authorInput" class="form-label">Author</label>
                <input type="text" class="form-control" id="authorInput" name="author"
                value = "<%=book.author%>" required>
            </div>
            <div class="mb-1">
                <label for="ratingInput" class="form-label">Rating</label>
                <div class="input-group">
                    <input type="number" class="form-control"  value = "<%=book.rating%>" id="ratingInput" name="rating" min="0" max="10" required>
                    <span class="input-group-text">/10</span>
                </div>
            </div>
            <div class="mb-1">
                <label for="shortDescInput" class="form-label">Short Description</label>
                <textarea class="form-control" 
                id="shortDescInput" name="shortDescription" rows="2" required><%=book.summary%></textarea>
                <div id="shortDescHelp" class="form-text">This will be shown on the home page.</div>
            </div>
            <div class="mb-1">
                <label for="revNotesInput" class="form-label">Review and Notes</label>
                <textarea class="form-control" 
                id="revNotesInput" name="reviewNotes" rows="8" required><%=book.notes_and_rev%></textarea>
            </div>
            <div class="mb-3">
                <label for="isbnInput" class="form-label">ISBN</label>
                <input type="text" class="form-control" id="isbnInput" name="isbn" value = "<%=book.isbn%>" required>
                <div id="isbnHelp" class="form-text">This is required to show the book's cover.</div>
            </div>
            <button type="submit" class="btn btn-primary">Save</button>
        </form>
    </div>
</body>
</html>
